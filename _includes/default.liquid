<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    {% render 'headfiles' %}
  </head>
  <body>
    <header>
      {% assign navItems = collections.main | eleventyNavigation | eleventyNavigationToHtml %}
      {% render 'nav', navItems: navItems %}
    </header>
    {{ content }}
    {% render 'scriptfiles' %}
  </body>
</html>